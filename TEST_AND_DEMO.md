# Coin Agent 테스트 및 데모 시나리오

## 문서 목적

이 문서는 핵심 기능 테스트, 정책 validation 테스트, 리스크 룰 테스트, Agent 출력 스키마 테스트, 외부 API 실패 테스트, 페이퍼 실행 테스트, E2E 테스트, 발표 데모 시나리오를 정의한다.

## 관련 문서

- 요구사항: `SPEC.md`
- 데이터 계약: `DATA.md`
- 역할별 구현 기준: `FE.md`, `BE.md`, `AI.md`

## 1. 테스트 원칙

- 실제 자동매매가 아니라 페이퍼 실행 중심 MVP를 검증한다.
- 정책 또는 리스크 게이트 불통과 시 무거래 또는 판단 보류가 되는지를 우선 검증한다.
- 외부 API 실패를 성공으로 처리하지 않는지 검증한다.
- Agent 출력은 자연어 품질보다 구조화 안정성을 우선 검증한다.

## 2. 핵심 기능 테스트

| 테스트 ID | 대상 | 검증 내용 | 관련 요구사항 |
|---|---|---|---|
| T-01 | 정책 저장 | 유효한 정책 저장 성공 | FR-01, FR-02 |
| T-02 | 시장 요약 조회 | 시세/지표 응답 정상 조회 | FR-03, FR-04 |
| T-03 | 리스크 판정 | 조건 충족/불충족 분기 정확성 | FR-05, FR-07 |
| T-04 | 자동 대응 후보 | 후보 행동 및 설명 생성 | FR-06, FR-09 |
| T-05 | 페이퍼 실행 | 실행 허용 시 모의 실행 결과 생성 | FR-08, FR-12 |
| T-06 | 리포트 조회 | 실행 결과 반영 리포트 제공 | FR-09, FR-10 |

## 3. 정책 Validation 테스트

- 코인 목록 누락 시 저장 실패
- 손절 값 음수 입력 시 저장 실패
- 주문 한도 0 입력 시 저장 실패
- 지원하지 않는 코인 입력 시 저장 실패 또는 보류
- 자동 대응 규칙 누락 시 저장 실패

## 4. 리스크 룰 테스트

- 손실 한도 접근 시 실행 차단
- 허용되지 않은 코인 매수 시도 시 차단
- 허용 주문 금액 초과 시 차단
- 데이터 부족 시 차단
- 급락 조건 충족 + 정책 허용 시 후보 생성

## 5. Agent 출력 스키마 테스트

- Policy Agent가 구조화 정책 객체를 반환하는지 검증
- Risk Agent가 `risk_level`, `gate_passed`, `blocked_reasons`를 포함하는지 검증
- Execution/Report Agent가 보류 사유 또는 실행 결과를 항상 포함하는지 검증
- LLM 응답이 스키마를 벗어나면 실패 처리되는지 검증

## 6. Upbit API 실패 테스트

- 네트워크 실패 시 오류 응답 반환
- 429 응답 시 재시도 후 차단 처리
- 빈 응답 또는 비정상 응답 시 신규 실행 보류
- 마지막 정상 상태가 있으면 FE에 함께 전달

## 7. 페이퍼 실행 테스트

- 허용된 후보에 대해 simulated execution 기록 생성
- 차단된 후보는 `blocked` 상태로 기록
- 실제 주문 경로가 호출되지 않음을 검증
- 로그와 리포트에 결과가 반영되는지 검증

## 8. E2E 테스트

### E2E-01 정책 저장 후 상태 확인

1. 정책 입력
2. 저장 성공
3. 대시보드 이동
4. 상태 카드 표시 확인

### E2E-02 급락 조건 평가와 실행 보류

1. 급락 시나리오 데이터 로드
2. 평가 요청
3. 리스크 게이트 실패
4. 자동 대응 카드에 보류 사유 표시
5. 실행 로그에 `blocked` 기록 확인

### E2E-03 허용 조건에서 페이퍼 실행

1. 허용 정책 로드
2. 평가 요청
3. 실행 허용 후보 생성
4. 페이퍼 실행 기록 생성
5. 리포트 화면 반영 확인

## 9. 발표 데모 시나리오

### 데모 1. 아침 위험 상태 점검

- 정책이 저장된 상태로 대시보드 진입
- BTC/ETH 상태 카드 확인
- 위험도와 자동 대응 대기 상태 설명

### 데모 2. 급락 발생과 자동 대응 보류

- 미리 준비한 급락 데이터 로드
- 자동 대응 후보가 생성되지만 리스크 게이트로 보류되는 흐름 시연
- “왜 실행되지 않았는가” 설명 강조

### 데모 3. 허용 조건의 페이퍼 실행과 리포트

- 실행 가능한 조건 데이터 로드
- 페이퍼 실행 결과 확인
- 로그와 리포트 화면으로 마무리

## 10. 데모용 초기 데이터

- 기본 정책 1세트
- BTC, ETH 샘플 MarketSnapshot
- 급락 케이스 1세트
- 실행 허용 케이스 1세트
- API 실패 케이스 1세트

## 11. 실패 시 백업 플랜

- Upbit 실시간 호출 실패 시 사전 저장된 샘플 MarketSnapshot 사용
- AI 응답 실패 시 사전 저장된 구조화 예시 결과 사용
- FE 렌더링 문제 발생 시 API 응답 JSON을 직접 보여주는 백업 절차 준비
- 네트워크 불안정 시 로컬 단일 환경 데모로 전환

## 12. 데모 준비 체크리스트

- 정책 저장 성공 확인
- 급락/허용 데이터 세트 준비
- 로그/리포트 화면 데이터 존재 확인
- 외부 API 실패 시나리오 수동 전환 방법 준비
- 실제 자동매매 미사용 문구 명시

## 13. MVP 기준 제안

- 테스트 우선순위는 정책 validation → 리스크 룰 → 실행 차단 → 페이퍼 실행 → 리포트 순으로 둔다.
- E2E는 3개 핵심 시나리오만 우선 보장한다.

## 결정 필요 사항

- **MVP 기준 제안**으로 Upbit 실시간 실패 시 샘플 데이터를 자동 fallback할지, 수동 전환할지 확정이 필요하다.
- **MVP 기준 제안**으로 AI 실패 백업 결과를 정적 JSON으로 둘지 확정이 필요하다.
- **MVP 기준 제안**으로 데모에서 private API 관련 화면을 아예 숨길지 확정이 필요하다.
