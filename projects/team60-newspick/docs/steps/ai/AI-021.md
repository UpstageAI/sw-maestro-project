---
id: AI-021
milestone: M5
requires:
  completed: [AI-020]
  external:
    mockable: []
    hard: []
---

# AI-021 - Keyword cloud 생성

## 목표
Daily Report 핵심 키워드와 weight를 생성한다.

## 사전 조건
- 같은 트랙 선행 step: AI-020 완료.
- mockable 외부 의존성 없음.
- hard 외부 의존성 없음.

## 파일 대상
```text
[신규] ai/src/newspick_ai/report/keywords.py
[신규] ai/tests/test_report_keywords.py
```

## 테스트 명세
- **테스트 파일**: `ai/tests/test_report_keywords.py`
- **테스트 이름**: `keyword_generator_returns_unique_keywords_with_weight_1_to_5`
- **fixture**: article summary fixture 3개는 `AI`, `반도체`, `환율` 단어를 포함한다.
- **action**: `KeywordGenerator().generate(articles)`를 호출한다.
- **assertions**:
  - 반환 키워드는 중복이 없다.
  - 각 weight는 1 이상 5 이하 정수다.
  - 상위 keyword text가 `AI`이다.
- **mock strategy**: LLM을 사용하지 않는 deterministic keyword extractor를 사용한다.
- **out of scope**: 개인화 keyword scoring은 범위 밖이다.

## 구현 기준
- 최대 12개 keyword를 반환한다.
- 불용어 목록은 테스트 fixture 안에서 고정한다.

## 검증 방법
`cd ai; uv run pytest ai/tests/test_report_keywords.py`
