---
id: AI-015
milestone: M4
requires:
  completed: [AI-014]
  external:
    mockable: []
    hard: []
---

# AI-015 - QuizGenerator node

## 목표
기사 summary/content에서 O/X 퀴즈 후보를 생성한다.

## 사전 조건
- 같은 트랙 선행 step: AI-014 완료.
- mockable 외부 의존성 없음.
- hard 외부 의존성 없음.

## 파일 대상
```text
[신규] ai/src/newspick_ai/graph/quiz_generator.py
[신규] ai/tests/test_quiz_generator.py
```

## 테스트 명세
- **테스트 파일**: `ai/tests/test_quiz_generator.py`
- **테스트 이름**: `quiz_generator_creates_two_ox_questions`
- **fixture**: fake Solar가 JSON quiz 2개를 반환한다. article fixture는 title, summary, content를 가진다.
- **action**: `QuizGenerator(chat_client=fake).run(state)`를 실행한다.
- **assertions**:
  - article `quizzes` 길이가 2이다.
  - 각 quiz에 `id`, `question`, `answer`, `explanation`이 있다.
  - answer는 boolean이다.
- **mock strategy**: SolarChatClient fake를 사용한다. 실제 Solar API는 호출하지 않는다.
- **out of scope**: quiz 저장과 schema 검증은 AI-016~AI-017에서 다룬다.

## 구현 기준
- quiz id는 article id와 index로 deterministic하게 만든다.
- 질문은 한국어 문장으로 제한한다.

## 검증 방법
`cd ai; uv run pytest ai/tests/test_quiz_generator.py`
