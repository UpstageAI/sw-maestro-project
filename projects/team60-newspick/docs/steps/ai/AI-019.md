---
id: AI-019
milestone: M5
requires:
  completed: [AI-018]
  external:
    mockable: []
    hard: []
---

# AI-019 - Briefing 생성

## 목표
clustered article 목록에서 daily briefing headline과 summary를 생성한다.

## 사전 조건
- 같은 트랙 선행 step: AI-018 완료.
- mockable 외부 의존성 없음.
- hard 외부 의존성 없음.

## 파일 대상
```text
[신규] ai/src/newspick_ai/report/briefing.py
[신규] ai/tests/test_report_briefing.py
```

## 테스트 명세
- **테스트 파일**: `ai/tests/test_report_briefing.py`
- **테스트 이름**: `briefing_generator_returns_headline_and_summary`
- **fixture**: cluster fixture 2개와 fake Solar 응답 `{headline:"오늘의 핵심", summary:"세 가지 흐름"}`를 사용한다.
- **action**: `BriefingGenerator(chat_client=fake).generate(clusters)`를 호출한다.
- **assertions**:
  - headline이 `오늘의 핵심`이다.
  - summary가 `세 가지 흐름`이다.
  - fake prompt에 cluster title 2개가 포함된다.
- **mock strategy**: SolarChatClient fake를 사용한다. 실제 Solar API는 호출하지 않는다.
- **out of scope**: timeline/flow/keywords는 후속 step에서 생성한다.

## 구현 기준
- headline은 40자 이하로 검증한다.
- summary는 500자 이하로 검증한다.

## 검증 방법
`cd ai; uv run pytest ai/tests/test_report_briefing.py`
