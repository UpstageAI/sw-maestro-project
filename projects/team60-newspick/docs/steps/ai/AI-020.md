---
id: AI-020
milestone: M5
requires:
  completed: [AI-019]
  external:
    mockable: []
    hard: []
---

# AI-020 - Timeline/flow 생성

## 목표
Daily Report timeline과 flow step을 생성한다.

## 사전 조건
- 같은 트랙 선행 step: AI-019 완료.
- mockable 외부 의존성 없음.
- hard 외부 의존성 없음.

## 파일 대상
```text
[신규] ai/src/newspick_ai/report/timeline_flow.py
[신규] ai/tests/test_report_timeline_flow.py
```

## 테스트 명세
- **테스트 파일**: `ai/tests/test_report_timeline_flow.py`
- **테스트 이름**: `timeline_flow_generator_returns_ordered_timeline_and_three_flow_steps`
- **fixture**: article fixture 3개는 publishedAt 09:00, 12:00, 18:00이고 fake Solar flow는 원인/전개/전망 3개다.
- **action**: `TimelineFlowGenerator(chat_client=fake).generate(articles)`를 호출한다.
- **assertions**:
  - timeline item이 publishedAt 오름차순이다.
  - flow steps label이 `원인`, `전개`, `전망` 순서다.
  - 각 timeline item은 `articleId`, `timeLabel`, `title`을 가진다.
- **mock strategy**: SolarChatClient fake를 사용한다. 날짜 정렬은 순수 함수로 검증한다.
- **out of scope**: keyword cloud는 AI-021에서 구현한다.

## 구현 기준
- timeLabel은 Asia/Seoul `HH:mm`로 고정한다.
- flow step은 정확히 3개를 반환한다.

## 검증 방법
`cd ai; uv run pytest ai/tests/test_report_timeline_flow.py`
