---
id: AI-017
milestone: M4
requires:
  completed: [AI-016]
  external:
    mockable: []
    hard: []
---

# AI-017 - Quiz persist

## 목표
검증된 quiz 배열을 article detail에서 읽을 수 있는 JSON payload로 저장한다.

## 사전 조건
- 같은 트랙 선행 step: AI-016 완료.
- mockable 외부 의존성 없음.
- hard 외부 의존성 없음.

## 파일 대상
```text
[신규] ai/src/newspick_ai/graph/quiz_persistor.py
[신규] ai/tests/test_quiz_persistor.py
```

## 테스트 명세
- **테스트 파일**: `ai/tests/test_quiz_persistor.py`
- **테스트 이름**: `quiz_persistor_updates_article_quiz_payload`
- **fixture**: Testcontainers DB에 `article_001` row를 만들고 state quiz 2개를 넣는다.
- **action**: `QuizPersistor.run(state)` 후 DB quiz payload를 조회한다.
- **assertions**:
  - DB payload 배열 길이가 2이다.
  - 첫 quiz id가 `quiz_001`이다.
  - answer가 JSON boolean true로 저장된다.
- **mock strategy**: Testcontainers PostgreSQL과 asyncpg를 사용한다. Solar는 호출하지 않는다.
- **out of scope**: BE DTO mapping은 BE-014에서 구현한다.

## 구현 기준
- 저장 컬럼은 기존 article JSON payload 컬럼 또는 확정된 quiz 컬럼을 사용한다.
- 같은 article 재실행은 payload를 덮어쓴다.

## 검증 방법
`cd ai; uv run pytest ai/tests/test_quiz_persistor.py`
