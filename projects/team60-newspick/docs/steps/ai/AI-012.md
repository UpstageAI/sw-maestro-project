---
id: AI-012
milestone: M3
requires:
  completed: [AI-011]
  external:
    mockable: []
    hard: []
---

# AI-012 - 요약 저장 업데이트

## 목표
검증된 summary와 content를 `articles` 테이블에 업데이트한다.

## 사전 조건
- 같은 트랙 선행 step: AI-011 완료.
- mockable 외부 의존성 없음.
- hard 외부 의존성 없음.

## 파일 대상
```text
[신규] ai/src/newspick_ai/graph/article_updater.py
[신규] ai/tests/test_article_updater.py
```

## 테스트 명세
- **테스트 파일**: `ai/tests/test_article_updater.py`
- **테스트 이름**: `article_updater_updates_content_summary_and_status`
- **fixture**: Testcontainers DB에 `article_001` status `collected` row를 넣고 state에는 content, summary, status `summarized`를 둔다.
- **action**: `ArticleUpdater.run(state)` 실행 후 DB row를 조회한다.
- **assertions**:
  - DB `content`가 state content와 같다.
  - DB `summary`가 state summary와 같다.
  - DB `status`가 `summarized`이다.
- **mock strategy**: Testcontainers PostgreSQL과 asyncpg를 사용한다. Solar는 호출하지 않는다.
- **out of scope**: embedding 저장은 AI-013에서 구현한다.

## 구현 기준
- 업데이트 대상은 id 기준이다.
- row가 없으면 skipped event를 남긴다.

## 검증 방법
`cd ai; uv run pytest ai/tests/test_article_updater.py`
