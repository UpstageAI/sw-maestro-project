---
id: FE-011
milestone: M3
requires:
  completed: [FE-010]
  external:
    mockable: [BE-009, AI-009]
    hard: []
---

# FE-011 - Article detail page 레이아웃

## 목표
상세 페이지에서 기사 제목, 출처, 날짜, 요약, 본문을 읽기 좋은 구조로 보여준다.

## 사전 조건
- 같은 트랙 선행 step: FE-010 완료.
- mockable 외부 의존성: BE-009, AI-009. 미완료 상태에서도 MSW 또는 fixture로 진행한다.
- hard 외부 의존성 없음.

## 파일 대상
```text
[신규] frontend/src/pages/ArticleDetailPage.tsx
[신규] frontend/src/pages/ArticleDetailPage.test.tsx
[수정] frontend/src/App.tsx
```

## 테스트 명세
- **테스트 파일**: `frontend/src/pages/ArticleDetailPage.test.tsx`
- **테스트 이름**: `article_detail_page_renders_title_summary_and_body`
- **fixture**: MSW `GET /api/articles/article_001` 응답은 title `AI가 바꾼 뉴스 소비`, summary `핵심 요약`, content paragraphs `본문 첫 문단

본문 둘째 문단`을 포함한다.
- **action**: MemoryRouter initial entry `/articles/article_001`로 렌더링한다.
- **assertions**:
  - heading level 1 이름이 `AI가 바꾼 뉴스 소비`이다.
  - 텍스트 `핵심 요약`이 보인다.
  - 본문 첫 문단과 본문 둘째 문단이 각각 보인다.
  - 뒤로가기 button role 이름이 `피드로 돌아가기`이다.
- **mock strategy**: MSW 상세 handler를 사용한다. 실제 BE/AI 서버는 호출하지 않는다.
- **out of scope**: 퀴즈, 관련 기사, live contract는 이 단계 범위가 아니다.

## 구현 기준
- route path는 `/articles/:articleId`로 고정한다.
- 본문 줄바꿈은 paragraph로 분리한다.

## 검증 방법
`cd frontend; pnpm test -- ArticleDetailPage.test.tsx`
`cd frontend; pnpm lint`

## 수동 브라우저 검증
- /articles/article_001에서 heading, summary, 본문 문단, 뒤로가기 버튼이 보인다.
