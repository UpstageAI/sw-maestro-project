---
id: FE-015
milestone: M3
requires:
  completed: [FE-014]
  external:
    mockable: [BE-009, AI-009]
    hard: []
---

# FE-015 - 본문 typography와 metadata polish

## 목표
기사 상세 화면의 본문 읽기 밀도, 날짜/출처/카테고리 표시를 완성한다.

## 사전 조건
- 같은 트랙 선행 step: FE-014 완료.
- mockable 외부 의존성: BE-009, AI-009. 미완료 상태에서도 MSW 또는 fixture로 진행한다.
- hard 외부 의존성 없음.

## 파일 대상
```text
[수정] frontend/src/pages/ArticleDetailPage.tsx
[수정] frontend/src/styles/article-detail.css
[수정] frontend/src/pages/ArticleDetailPage.test.tsx
```

## 테스트 명세
- **테스트 파일**: `frontend/src/pages/ArticleDetailPage.test.tsx`
- **테스트 이름**: `article_detail_page_renders_metadata_and_readable_paragraphs`
- **fixture**: detail fixture는 `source: Example News`, `category: tech`, `publishedAt: 2026-05-12T00:00:00Z`, 본문 3문단을 가진다.
- **action**: 상세 페이지를 렌더링한다.
- **assertions**:
  - 텍스트 `Example News`가 보인다.
  - 텍스트 `테크`가 보인다.
  - paragraph role 또는 `p` 요소가 3개이다.
  - 본문 container class가 max width token을 사용한다.
- **mock strategy**: MSW detail fixture를 사용한다. CSS 검증은 DOM class와 snapshot이 아니라 사용자 표시 텍스트 중심으로 한다.
- **out of scope**: 라이브 API와 퀴즈 영역은 제외한다.

## 구현 기준
- 날짜 format은 `2026.05.12`로 고정한다.
- 모바일에서 본문 줄 길이가 과도하게 길어지지 않게 max-width를 둔다.

## 검증 방법
`cd frontend; pnpm test -- ArticleDetailPage.test.tsx`
`cd frontend; pnpm lint`

## 수동 브라우저 검증
- /articles/article_001에서 출처, 날짜, 카테고리, 3문단이 겹치지 않고 보인다.
