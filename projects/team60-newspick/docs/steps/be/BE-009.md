---
id: BE-009
milestone: M3
requires:
  completed: [BE-008]
  external:
    mockable: []
    hard: []
---

# BE-009 - Feed DTO content preview 확장

## 목표
feed 목록 DTO에 summary preview와 thumbnail nullable 필드를 추가한다.

## 사전 조건
- 같은 트랙 선행 step: BE-008 완료.
- mockable 외부 의존성 없음.
- hard 외부 의존성 없음.

## 파일 대상
```text
[수정] backend/src/main/java/com/newspick/feed/FeedResponse.java
[수정] backend/src/main/java/com/newspick/feed/FeedService.java
[신규] backend/src/test/java/com/newspick/feed/FeedDtoTest.java
```

## 테스트 명세
- **테스트 파일**: `backend/src/test/java/com/newspick/feed/FeedDtoTest.java`
- **테스트 이름**: `feed_response_includes_summary_preview_and_nullable_thumbnail`
- **fixture**: Article fixture `article_001`은 summary `긴 요약 문장입니다`, thumbnailUrl null을 가진다.
- **action**: FeedService mapper를 호출해 FeedResponse로 변환한다.
- **assertions**:
  - `summaryPreview`가 `긴 요약 문장입니다`이다.
  - `thumbnailUrl`이 null로 직렬화된다.
  - JSON에 `id`, `title`, `source`, `publishedAt`이 남아 있다.
- **mock strategy**: Repository mock을 사용한다. DB는 호출하지 않는다.
- **out of scope**: article detail content 전체 응답은 BE-010에서 구현한다.

## 구현 기준
- openapi field 이름을 그대로 사용한다.
- preview 길이 제한은 120자로 고정한다.

## 검증 방법
`cd backend; ./gradlew test --tests "*FeedDtoTest"`
